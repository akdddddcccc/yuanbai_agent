# 元白长者资料补充清单

公开、长期有效的资料由语料负责人整理后提交到发布仓库的 `edge-functions/_shared/yuanbai-knowledge.js`。路演当天临时使用的 PDF、Word、PPT 和文本文件，可以在“对话元白”右上角的“资料”抽屉中加入；临时资料只存当前浏览器，原文件不会上传。

## 1. 元白楼

优先补充：

- 2004 年前后投入使用时的原始用途、设计者和旧照片；
- 2019 年后改造的完整时间线、设计团队、施工限制和关键决策；
- A/B 区、楼层、教室、实验室、展厅和屋顶流线的准确对应关系；
- 红砖、GRC、U 玻、钢楼梯、屋顶花园等材料和构造的现场故事；
- 师生真正使用某个空间的故事，例如展览、课程、偶遇和空间问题；
- 平面图、剖面图、导视图及其公开授权情况。

## 2. 班级与人员

每条人物资料建议包含：

- 姓名、年级、班级、角色；
- 擅长的方向、正在做的项目、可公开的小特点；
- 辅导员、班主任、课程教师等关系；
- 资料来源、确认日期、是否本人同意公开；
- 可见范围：公开、仅路演、仅团队；
- 不应回答的隐私，例如具体床位、联系方式、健康和家庭信息。

请不要只给一整张名单。每个人至少需要一项能帮助自然对话的真实特点，并标明玩笑、事实或待核验。

## 3. 学院作品与项目

每个项目用一页资料最合适：

- 项目名称、年份、课程或展览；
- 指导教师和学生团队；
- 面对的真实问题与目标用户；
- 调研方法、设计过程和关键转折；
- 原型、测试、最终作品和实际反馈；
- 图片说明、作品链接、是否允许在路演中展示；
- 失败或未解决的问题。它们往往比获奖介绍更适合元白长者回答设计困惑。

## 4. 设计方法与倾诉场景

建议收集 10 至 20 个真实案例，例如：

- 想法太多，无法收敛；
- 找不到清楚的设计问题；
- 调研很多，但无法转化成概念；
- 作品只有形式，没有使用者；
- 原型被老师否定；
- 小组意见冲突；
- 截止日期临近，作品仍不完整；
- 做不出效果图或讲不清项目；
- 与同学比较后怀疑自己的能力。

每个案例可以写“当时发生了什么、什么回应有帮助、什么说法令人反感、最后采取了哪一步”。这会比抽象的心理安慰更有用。

## 5. 每份资料的最小元数据

```text
标题：
内容类型：官方资料 / 教师确认 / 学生口述 / 校园玩笑 / 待核验
来源：
确认日期：
可见范围：公开 / 仅路演 / 仅团队
有效期：长期 / 某学期 / 某一天
正文：
```

涉及师生个人的信息，应先得到本人同意，再进入公开网站的长期语料。课程安排、辅导员和人员职务会变化，要带确认日期并定期更新。

## 6. 现场多设备共用的合成名单测试

普通上传资料只保存在上传它的浏览器。要让现场手机和电脑共用一份名单，合成测试名单写入 EdgeOne Pages KV，由元白服务端按姓名读取；名单数据不提交到 Git，也不打进前端构建。

云端 KV 仅用于这次明确标记为合成的测试数据。JSON 顶层格式为：

```json
{
  "synthetic": true,
  "records": [
    { "name": "示例同学", "gender": "女", "class": "26级1班" }
  ]
}
```

每条记录只允许姓名、性别、班级；不要加入学号、联系方式、住宿、团务或其他字段。当前查询按姓名精确匹配，班级是默认回答字段，性别只在明确询问时回答。若以后替换成真实名单，先另行确认访问控制与留存策略，不复用本次测试库。

发布项目需在 EdgeOne Pages KV 中创建命名空间并绑定为 `YUANBAI_ROSTER`，再设置仅管理员持有的 `YUANBAI_ROSTER_ADMIN_TOKEN`。代码部署后，用发布仓库中的 `scripts/upload-yuanbai-synthetic-roster.mjs` 上传本地 JSON；上传命令只报告记录条数，不会把名单写入 Git。KV 更新在不同边缘节点间最多可能有约 60 秒同步延迟。

